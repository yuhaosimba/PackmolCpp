Project conventions: preserve the Fortran reference behavior and lock it with tests before changing behavior. Prefer small focused tests and refresh the procedure inventory when test coverage changes. Build warnings are present under GNU Fortran, but CMake only promotes warnings to errors in Debug builds. Code layout follows traditional Packmol structure: Fortran sources in src/, CLI in app/, tests/support for thin shims only, fixtures in tests/fixtures, and inventory artifacts in tests/inventory/. Use pixi tasks as the canonical workflow when possible.