When finishing PackmolCpp work, prefer this verification sequence: refresh inventory if interfaces or coverage changed, rebuild, run the narrowest affected tests first, then run broader verification as needed. Canonical commands are `pixi run inventory`, `pixi run build`, targeted `ctest --test-dir build --output-on-failure -R <name>` or `-L <label>`, and finally `pixi run test-all` for full baseline confidence.